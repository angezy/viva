/* Optional development fixtures. This file is intentionally separate from
   production bootstrap and contains no real customer or transaction data. */
SET NOCOUNT ON;
PRINT 'No development fixtures are enabled by default.';
